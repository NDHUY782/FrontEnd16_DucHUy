let agent=navigator.userAgent ;
console.log(agent);
const position = agent.search('Chorm');
console.log(agent.search('Chorm'));
if (agent.search('Chorm') !=1) {
    document.body.stlye.backgroundColor = "red" 
    
}
if else {
    
}